<?php
require_once __DIR__ . '/../includes/config.php';
// Destroy admin session flag and optionally session
unset($_SESSION['is_admin'], $_SESSION['admin_user']);

// Redirect to login
header('Location: /admin/login.php');
exit;